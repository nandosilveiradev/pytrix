import os
import time

def watch_project():
    print("Pytrix Watcher Active [Language: Auto-detected]")
    # Monitora o arquivo da aba ativa ou o .env
    # Se Alt+S for pressionado (detectado via arquivo de sinal ou socket)
    # Executa as FLAGS do .env e mostra o resultado aqui em baixo.
    
if __name__ == "__main__":
    watch_project()